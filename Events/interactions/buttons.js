let Buttons = { // Buttons custom_id's and their application commands references, this is hardcoded to call button click function in the same place as the command with an executeButton function
    "eu": "roles",
    "na": "roles",
    "kr": "roles",
    "clean": "roles",
    "bnetauth": "authorize",
}

module.exports = {Buttons};